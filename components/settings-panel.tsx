// Built by PRANAV PANDEY
'use client'
import { useEffect, useState } from 'react'
export function SettingsPanel() { const [theme, setTheme] = useState('system'); const [animations, setAnimations] = useState(true); const [notifications, setNotifications] = useState(true); useEffect(() => { if (theme === 'dark') document.documentElement.classList.add('dark'); else if (theme === 'light') document.documentElement.classList.remove('dark'); else document.documentElement.classList.toggle('dark', window.matchMedia('(prefers-color-scheme: dark)').matches) }, [theme]); return <div className="max-w-2xl"><h1 className="text-3xl font-semibold tracking-tight">Settings</h1><p className="mt-2 text-muted-foreground">Tune your workspace preferences.</p><div className="mt-8 flex flex-col gap-4 rounded-2xl border border-border bg-card p-6"><label className="flex items-center justify-between gap-4"><span><span className="block font-medium">Theme</span><span className="text-sm text-muted-foreground">Choose your preferred appearance.</span></span><select value={theme} onChange={(event) => setTheme(event.target.value)} className="rounded-lg border border-input bg-background px-3 py-2"><option value="system">System</option><option value="light">Light</option><option value="dark">Dark</option></select></label>{[['animations', 'Animations', animations, setAnimations], ['notifications', 'Notifications', notifications, setNotifications]].map(([key, label, value, setter]) => <label key={key as string} className="flex items-center justify-between border-t border-border pt-4"><span className="font-medium">{label as string}</span><input type="checkbox" checked={value as boolean} onChange={(event) => (setter as (value: boolean) => void)(event.target.checked)} className="size-5 accent-primary" /></label>)}</div><div className="mt-4 rounded-2xl border border-border bg-card p-6"><h2 className="font-semibold">API status</h2><p className="mt-2 text-sm text-muted-foreground">URL scanning service connected. Password analysis and file encryption run locally.</p></div></div> }
